type Book = {
    id: string,
    title: string,
    author: string;
    category: string,
}

let books: Book[] = [];

export function addBook(book: Book) {

}

export function reportLostBook(books: Book[]) {
    
}